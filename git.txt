#clone repo
git clone (repo URL from github)

#Pull repo and rebase existing files
git pull --rebase

#Add file to git commit
git add (filename)

#commit changes with a short commit message
git commit -m "message"

#commit changes with a longer commit message (prompt)
git commit -a

#push changes to branch
git push origin (branch)
